#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie();

    ll i,j,k;
    ll u,v,n;
    ll l,r;

    cin>>l>>r;

    if(l==r)
        cout<<l;
    else
        cout<<"2";

    cin.clear();
    cin.ignore();
    //cin.get();

    return 0;
}